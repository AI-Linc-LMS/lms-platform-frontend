import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
	appId: 'com.ailinc.lms',
	appName: 'AI Linc',
	webDir: 'dist',
	server: {
		androidScheme: 'https'
	}
};

export default config; 